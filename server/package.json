{
  "name": "@rl-overlay/server",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -r dist/* && bun --bun build --compile --minify --sourcemap ./src/index.ts --outfile dist/VBARL",
    "dev": "bun dev"
  },
  "dependencies": {
    "webview-bun": "^2.3.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.17"
  }
}
